import 'package:flutter/material.dart';

class SchemaTab extends StatelessWidget {
  const SchemaTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
